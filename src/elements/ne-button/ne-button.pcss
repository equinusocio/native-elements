@custom-selector :--icons i, svg;

button,
a[ne-button] {
  @mixin button;

  & :--icons {
    color: inherit;
    fill: currentColor;
    font-style: normal;
    margin-inline-end: env(--buttonIconMargin);
    height: env(--buttonIconSize);
  }
}

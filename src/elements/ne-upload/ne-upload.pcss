input[type='file'] {
  font-size: inherit;
  width: 100%;
  overflow: hidden;
  padding: env(--buttonOutlineWidth) 0 0 env(--buttonOutlineWidth);

  &::-webkit-file-upload-button {
    @mixin button;

    margin-right: 16px;
  }
}
